# Electron Crash Testing

## How to use?

change the `submitURL` property in `./src/config.ts` then

```sh
# install dependencies
$ npm install

# run the app
$ npm start
```

click the `Crash Main Process` button
